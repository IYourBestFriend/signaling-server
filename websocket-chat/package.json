{
  "name": "websocket-chat",
  "version": "1.0.0",
  "description": "A simple WebSocket-based chat server.",
  "dependencies": {
    "websocket": "*"
  },
  "scripts": {
    "watch": "nodemon ./chatserver.js --ignore node_modules/ --ignore index.html --ignore chatclient.js",
    "dev": "browser-sync start --server --files '**/*.css, **/*.html, **/*.js'  --port 7777"
  },
  "devDependencies": {
    "nodemon": "1.18.9"
  }
}
